# v0.4.3 Release

Date: 2026-05-19

## Scope

Release the P20 runtime retention cleanup surface.

## Release Commit And Tag

- Commit: `279c975872681ea6689769fec4f33e36f516dff4`
- Tag: `v0.4.3`
- GitHub release: `https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.4.3`

## Local Source Validation

Commands:

```bash
python -m compileall -q src tests .codex/hooks
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

Outcome:

- Unit tests: `65` tests passed.
- Compileall passed.

## Installed-Wheel Smoke

Commands:

```bash
python -m venv /tmp/codex-wake-v043-build
/tmp/codex-wake-v043-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v043-build/bin/python -m build
python -m venv /tmp/codex-wake-v043-install
/tmp/codex-wake-v043-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-v043-install/bin/python -m pip install dist/*.whl
/tmp/codex-wake-v043-install/bin/codex-wake --help
/tmp/codex-wake-v043-install/bin/codex-waked --once --no-dispatch --wake-root /tmp/codex-wake-v043-empty
printf '{"prompt":"hello","cwd":"%s"}' "$PWD" | /tmp/codex-wake-v043-install/bin/codex-wake-hook
```

Cleanup smoke:

```bash
ROOT=/tmp/codex-wake-v043-runtime
WAKE=$(/tmp/codex-wake-v043-install/bin/codex-wake --wake-root "$ROOT/wake" after 1m -- 'Release 0.4.3 cleanup smoke' | awk '{print $1}')
/tmp/codex-wake-v043-install/bin/codex-wake --wake-root "$ROOT/wake" cancel "$WAKE"
/tmp/codex-wake-v043-install/bin/codex-wake --wake-root "$ROOT/wake" cleanup --archive-terminal --older-than 30d
# archived_at was set to 2026-04-01T00:00:00Z for retention smoke
/tmp/codex-wake-v043-install/bin/codex-wake --wake-root "$ROOT/wake" cleanup --older-than 30d
/tmp/codex-wake-v043-install/bin/codex-wake --wake-root "$ROOT/wake" cleanup --older-than 30d --delete
/tmp/codex-wake-v043-install/bin/codex-wake --wake-root "$ROOT/wake" list --archived
```

Outcomes:

- Built `codex_wake-0.4.3.tar.gz`.
- Built `codex_wake-0.4.3-py3-none-any.whl`.
- Installed wheel reported `codex-wake==0.4.3`.
- Installed-wheel smokes passed for `codex-wake`, `codex-waked`, and `codex-wake-hook`.
- Cleanup smoke archived a cancelled wake, previewed deletion in dry-run mode, deleted only after `--delete`, and ended with no archived wakes.

## Public Tag Install Smoke

Commands:

```bash
BASE=/tmp/codex-wake-v043-clean
export UV_TOOL_DIR="$BASE/tools"
export UV_TOOL_BIN_DIR="$BASE/bin"
export UV_CACHE_DIR="$BASE/cache"
export PATH="$BASE/bin:$PATH"
uv tool install --force --reinstall 'git+https://github.com/CochranResearchGroup/codex-wake.git@v0.4.3'
command -v codex-wake
command -v codex-waked
command -v codex-wake-hook
codex-wake --help
codex-waked --once --no-dispatch --wake-root "$BASE/runtime/empty"
printf '{"prompt":"hello","cwd":"%s"}' "$BASE/repo" | codex-wake-hook
```

Cleanup smoke from the clean tag install:

```text
archived wake_20260520_012721_ae59 /tmp/codex-wake-v043-clean/runtime/wake/archive/wake_20260520_012721_ae59.json
cleanup mode=dry-run older_than=30d archived=1 matched=0
would-delete wake_20260520_012721_ae59 ... retention_at=2026-04-01T00:00:00Z
cleanup mode=dry-run older_than=30d archived=0 matched=1
deleted wake_20260520_012721_ae59 ... retention_at=2026-04-01T00:00:00Z
cleanup mode=delete older_than=30d archived=0 matched=1
No wakes.
```

Outcomes:

- Public tag resolved to `279c975872681ea6689769fec4f33e36f516dff4`.
- Installed `codex-wake==0.4.3`.
- Installed executables: `codex-wake`, `codex-waked`, `codex-wake-hook`.

## Upstream CI

- Run: `26135597539`
- URL: `https://github.com/CochranResearchGroup/codex-wake/actions/runs/26135597539`
- Result: success
- Commit: `279c975872681ea6689769fec4f33e36f516dff4`

## Result

Pass. `v0.4.3` is published and installable from the public GitHub tag.
