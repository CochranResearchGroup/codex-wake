# v0.4.8 Release

Date: 2026-05-20

## Scope

Release the P26 `cleanup --json` automation surface.

## Release Commit And Tag

- Commit: `cebb92cae0ee62939bceb309d43f98dfdddb90b9`
- Tag: `v0.4.8`
- GitHub release: `https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.4.8`

## Local Source Validation

Commands:

```bash
python -m compileall -q src tests .codex/hooks
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

Outcome:

- Unit tests: `74` tests passed.
- Compileall passed.

## Installed-Wheel Smoke

Commands:

```bash
python -m venv /tmp/codex-wake-v048-build
/tmp/codex-wake-v048-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v048-build/bin/python -m build --outdir /tmp/codex-wake-v048-dist
python -m venv /tmp/codex-wake-v048-install
/tmp/codex-wake-v048-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-v048-install/bin/python -m pip install /tmp/codex-wake-v048-dist/*.whl
WAKE=$(PATH=/tmp/codex-wake-v048-install/bin:$PATH /tmp/codex-wake-v048-install/bin/codex-wake --wake-root /tmp/codex-wake-v048-runtime/wake after --app-server-thread-id thread_cleanup 1m -- 'Release cleanup json smoke' | awk '{print $1}')
PATH=/tmp/codex-wake-v048-install/bin:$PATH /tmp/codex-wake-v048-install/bin/codex-wake --wake-root /tmp/codex-wake-v048-runtime/wake cancel "$WAKE"
PATH=/tmp/codex-wake-v048-install/bin:$PATH /tmp/codex-wake-v048-install/bin/codex-wake --wake-root /tmp/codex-wake-v048-runtime/wake cleanup --archive-terminal --older-than 30d --json
/tmp/codex-wake-v048-install/bin/python -m pip show codex-wake
```

Outcomes:

- Built `codex_wake-0.4.8.tar.gz`.
- Built `codex_wake-0.4.8-py3-none-any.whl`.
- Installed wheel reported `codex-wake==0.4.8`.
- Installed CLI created and cancelled a wake.
- Installed `cleanup --archive-terminal --json` emitted valid JSON.
- JSON reported `archive_terminal=true`.
- JSON reported `archived_terminal_count=1`.
- JSON reported `mode=dry-run`.
- JSON reported `matched_count=0` for the newly archived record under the 30-day retention window.

## Public Tag Install Smoke

Commands:

```bash
BASE=/tmp/codex-wake-v048-clean
export UV_TOOL_DIR="$BASE/tools"
export UV_TOOL_BIN_DIR="$BASE/bin"
export UV_CACHE_DIR="$BASE/cache"
export PATH="$BASE/bin:$PATH"
uv tool install --force --reinstall 'git+https://github.com/CochranResearchGroup/codex-wake.git@v0.4.8'
command -v codex-wake
command -v codex-waked
command -v codex-wake-hook
WAKE=$(codex-wake --wake-root "$BASE/runtime/wake" after --app-server-thread-id thread_cleanup 1m -- 'Public cleanup json smoke' | awk '{print $1}')
codex-wake --wake-root "$BASE/runtime/wake" cancel "$WAKE"
codex-wake --wake-root "$BASE/runtime/wake" cleanup --archive-terminal --older-than 30d --json
codex-waked --once --no-dispatch --wake-root "$BASE/runtime/empty"
printf '{"prompt":"hello","cwd":"%s"}' "$BASE/repo" | codex-wake-hook
```

Outcomes:

- Public tag resolved to `cebb92cae0ee62939bceb309d43f98dfdddb90b9`.
- Installed `codex-wake==0.4.8`.
- Installed executables: `codex-wake`, `codex-waked`, `codex-wake-hook`.
- Public tag CLI created and cancelled a wake.
- Public tag `cleanup --archive-terminal --json` emitted valid JSON.
- JSON reported `archive_terminal=true`, `archived_terminal_count=1`, `mode=dry-run`, and `matched_count=0`.
- Daemon one-shot smoke reported no pending work for the empty root and exited successfully.
- Hook no-wake prompt smoke exited successfully.

## Upstream CI

- Run: `26183703882`
- URL: `https://github.com/CochranResearchGroup/codex-wake/actions/runs/26183703882`
- Result: success
- Commit: `cebb92cae0ee62939bceb309d43f98dfdddb90b9`

## Result

Pass. `v0.4.8` is published and installable from the public GitHub tag.
