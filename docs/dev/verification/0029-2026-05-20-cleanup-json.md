# Cleanup JSON Surface

Date: 2026-05-20

Lane: P26

## Scope

Add structured JSON output to `codex-wake cleanup` while preserving dry-run defaults and existing text output.

## Changes

- Added `codex-wake cleanup --json`.
- JSON output includes wake root, mode, retention window, whether terminal archiving was requested, archived terminal records, matched archived records, and counts.
- Existing cleanup text output remains unchanged.
- README now documents `cleanup --archive-terminal --json` and structured cleanup reports.

## Source Validation

Commands:

```bash
PYTHONPATH=src python -m unittest tests.test_cli.CliTests.test_cleanup_dry_run_and_delete_archived_records tests.test_cli.CliTests.test_cleanup_json_reports_archive_and_delete_candidates tests.test_cli.CliTests.test_cleanup_can_archive_terminal_records_first
ROOT=/tmp/codex-wake-p26-smoke/wake
rm -rf /tmp/codex-wake-p26-smoke
WAKE=$(PYTHONPATH=src python -m codex_wake.cli --wake-root "$ROOT" after --app-server-thread-id thread_cleanup 1m -- 'Cleanup json smoke' | awk '{print $1}')
PYTHONPATH=src python -m codex_wake.cli --wake-root "$ROOT" cancel "$WAKE"
PYTHONPATH=src python -m codex_wake.cli --wake-root "$ROOT" cleanup --archive-terminal --older-than 30d --json
python -m compileall -q src tests .codex/hooks
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

Outcomes:

- Focused cleanup tests passed.
- Source CLI `cleanup --archive-terminal --json` emitted valid JSON.
- Compileall passed.
- Unit tests: `74` tests passed.

## Installed-Wheel Smoke

Commands:

```bash
python -m venv /tmp/codex-wake-p26-build
/tmp/codex-wake-p26-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-p26-build/bin/python -m build --outdir /tmp/codex-wake-p26-dist
python -m venv /tmp/codex-wake-p26-install
/tmp/codex-wake-p26-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-p26-install/bin/python -m pip install /tmp/codex-wake-p26-dist/*.whl
WAKE=$(PATH=/tmp/codex-wake-p26-install/bin:$PATH /tmp/codex-wake-p26-install/bin/codex-wake --wake-root /tmp/codex-wake-p26-runtime/wake after --app-server-thread-id thread_cleanup 1m -- 'Installed cleanup json smoke' | awk '{print $1}')
PATH=/tmp/codex-wake-p26-install/bin:$PATH /tmp/codex-wake-p26-install/bin/codex-wake --wake-root /tmp/codex-wake-p26-runtime/wake cancel "$WAKE"
PATH=/tmp/codex-wake-p26-install/bin:$PATH /tmp/codex-wake-p26-install/bin/codex-wake --wake-root /tmp/codex-wake-p26-runtime/wake cleanup --archive-terminal --older-than 30d --json
```

Outcomes:

- Built and installed a local working-tree wheel.
- Installed CLI created and cancelled a wake.
- Installed `cleanup --archive-terminal --json` emitted valid JSON.
- JSON reported `archive_terminal=true`.
- JSON reported `archived_terminal_count=1`.
- JSON reported `mode=dry-run`.
- JSON reported `matched_count=0` for the newly archived record under the 30-day retention window.

## Known Gaps

- This lane does not add automatic cleanup.
- This lane does not change `status --json`; cleanup remains a separate explicit operator action.

## Result

Pass. P26 is closed.
