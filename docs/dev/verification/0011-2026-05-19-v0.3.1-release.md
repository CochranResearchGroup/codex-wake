# v0.3.1 Release

Date: 2026-05-19

## Scope

Patch release for stale pane-lock cleanup, hook visibility diagnostics, current-TUI dogfood proof, and CI release gates.

## Release Commit And Tag

- Commit: `aad1be4efb4cde676e03152af94e15214429e417`
- Tag: `v0.3.1`
- GitHub release: `https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.3.1`

## Local Source Validation

Commands:

```bash
python -m compileall -q src tests .codex/hooks
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
python -m venv /tmp/codex-wake-release-build
/tmp/codex-wake-release-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-release-build/bin/python -m build
python -m venv /tmp/codex-wake-release-install
/tmp/codex-wake-release-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-release-install/bin/python -m pip install dist/*.whl
/tmp/codex-wake-release-install/bin/codex-wake --help
/tmp/codex-wake-release-install/bin/codex-waked --once --no-dispatch --wake-root /tmp/codex-wake-release-empty
printf '{"prompt":"hello","cwd":"%s"}' "$PWD" | /tmp/codex-wake-release-install/bin/codex-wake-hook
TMUX_PANE='%11' TMUX='/tmp/tmux-1000/default,123,0' \
  /tmp/codex-wake-release-install/bin/codex-wake --wake-root /tmp/codex-wake-release-runtime after 1m -- "Release smoke wake"
/tmp/codex-wake-release-install/bin/codex-wake --wake-root /tmp/codex-wake-release-runtime list
```

Outcomes:

- Unit tests: `44` tests passed.
- Built `codex_wake-0.3.1.tar.gz`.
- Built `codex_wake-0.3.1-py3-none-any.whl`.
- Installed wheel reported `codex-wake==0.3.1`.
- `codex-waked --once --no-dispatch` returned an empty daemon pass.
- Non-wake `codex-wake-hook` smoke returned empty output.
- Tmux-env-only wake creation smoke wrote a pending wake without live dispatch.

## Public Tag Install Smoke

Commands:

```bash
BASE=/tmp/codex-wake-v031-clean
export UV_TOOL_DIR="$BASE/tools"
export UV_TOOL_BIN_DIR="$BASE/bin"
export UV_CACHE_DIR="$BASE/cache"
export PATH="$BASE/bin:$PATH"
uv tool install --force --reinstall 'git+https://github.com/CochranResearchGroup/codex-wake.git@v0.3.1'
command -v codex-wake
command -v codex-waked
command -v codex-wake-hook
codex-wake --help
codex-waked --once --no-dispatch --wake-root "$BASE/runtime/empty"
printf '{"prompt":"hello","cwd":"%s"}' "$BASE/repo" | codex-wake-hook
codex-wake --wake-root "$BASE/runtime/wake" hook install --repo-root "$BASE/repo"
codex-wake --wake-root "$BASE/runtime/wake" hook check --repo-root "$BASE/repo"
TMUX_PANE='%11' TMUX='/tmp/tmux-1000/default,123,0' \
  codex-wake --wake-root "$BASE/runtime/wake" after 1m -- 'Clean v0.3.1 smoke wake'
codex-wake --wake-root "$BASE/runtime/wake" list
codex-wake --wake-root "$BASE/runtime/wake" doctor --repo-root "$BASE/repo" --name codex-wake-v031-clean --log-path "$BASE/state/service.log" --interval 1
```

Outcomes:

- `uv tool install` resolved tag `v0.3.1` to commit `aad1be4efb4cde676e03152af94e15214429e417`.
- Installed `codex-wake==0.3.1`.
- Installed commands:
  - `/tmp/codex-wake-v031-clean/bin/codex-wake`
  - `/tmp/codex-wake-v031-clean/bin/codex-waked`
  - `/tmp/codex-wake-v031-clean/bin/codex-wake-hook`
- Hook install/check passed against a temporary repo.
- Clean wake creation smoke wrote a pending `not_before` wake.
- Doctor reported installed command paths, hook config installed, service inactive, and the hook visibility trust note.

Note: the first doctor command attempt placed `--wake-root` after `doctor`, which is invalid because `--wake-root` is a global option. The corrected command above passed.

## Upstream CI

- Run: `26102171944`
- URL: `https://github.com/CochranResearchGroup/codex-wake/actions/runs/26102171944`
- Result: success
- Commit: `aad1be4efb4cde676e03152af94e15214429e417`

## Result

Pass. `v0.3.1` is published and installable from the public GitHub tag.
