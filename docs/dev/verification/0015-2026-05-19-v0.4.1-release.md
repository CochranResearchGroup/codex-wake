# v0.4.1 Release

Date: 2026-05-19

## Scope

Release the P18 app-server wake hardening surface.

## Release Commit And Tag

- Commit: `2f53ea928690a2a7657dc9b3c4c1cb69c1016137`
- Tag: `v0.4.1`
- GitHub release: `https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.4.1`

## Local Source Validation

Commands:

```bash
python -m compileall -q src tests .codex/hooks
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

Outcome:

- Unit tests: `53` tests passed.
- Compileall passed.

## Installed-Wheel Smoke

Commands:

```bash
python -m venv /tmp/codex-wake-v041-build
/tmp/codex-wake-v041-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v041-build/bin/python -m build
python -m venv /tmp/codex-wake-v041-install
/tmp/codex-wake-v041-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-v041-install/bin/python -m pip install dist/*.whl
/tmp/codex-wake-v041-install/bin/codex-wake --help
/tmp/codex-wake-v041-install/bin/codex-waked --once --no-dispatch --wake-root /tmp/codex-wake-v041-empty
printf '{"prompt":"hello","cwd":"%s"}' "$PWD" | /tmp/codex-wake-v041-install/bin/codex-wake-hook
/tmp/codex-wake-v041-install/bin/codex-wake --wake-root /tmp/codex-wake-v041-runtime app after thread_release 1m -- 'Release app after'
/tmp/codex-wake-v041-install/bin/codex-wake --wake-root /tmp/codex-wake-v041-runtime app at thread_release '2026-05-19T17:30:00-05:00' -- 'Release app at'
/tmp/codex-wake-v041-install/bin/codex-wake --wake-root /tmp/codex-wake-v041-runtime list --json
```

Outcomes:

- Built `codex_wake-0.4.1.tar.gz`.
- Built `codex_wake-0.4.1-py3-none-any.whl`.
- Installed wheel reported `codex-wake==0.4.1`.
- Installed-wheel smokes passed for `codex-wake`, `codex-waked`, and `codex-wake-hook`.
- Installed-wheel `app after` and `app at` created pending app-server records with `transport: app-server`, `endpoint: stdio://`, and `thread_id: thread_release`.

## Public Tag Install Smoke

Commands:

```bash
BASE=/tmp/codex-wake-v041-clean
export UV_TOOL_DIR="$BASE/tools"
export UV_TOOL_BIN_DIR="$BASE/bin"
export UV_CACHE_DIR="$BASE/cache"
export PATH="$BASE/bin:$PATH"
uv tool install --force --reinstall 'git+https://github.com/CochranResearchGroup/codex-wake.git@v0.4.1'
command -v codex-wake
command -v codex-waked
command -v codex-wake-hook
codex-wake --help
codex-waked --once --no-dispatch --wake-root "$BASE/runtime/empty"
printf '{"prompt":"hello","cwd":"%s"}' "$BASE/repo" | codex-wake-hook
codex-wake --wake-root "$BASE/runtime/wake" app after thread_clean 1m -- 'Clean tag app after'
codex-wake --wake-root "$BASE/runtime/wake" app at thread_clean '2026-05-19T17:30:00-05:00' -- 'Clean tag app at'
codex-wake --wake-root "$BASE/runtime/wake" list --json
```

Outcomes:

- Public tag resolved to `2f53ea928690a2a7657dc9b3c4c1cb69c1016137`.
- Installed `codex-wake==0.4.1`.
- Installed executables: `codex-wake`, `codex-waked`, `codex-wake-hook`.
- Clean tag `app after` and `app at` created pending app-server records with `thread_id: thread_clean`.

## Upstream CI

- Run: `26127180628`
- URL: `https://github.com/CochranResearchGroup/codex-wake/actions/runs/26127180628`
- Result: success
- Commit: `2f53ea928690a2a7657dc9b3c4c1cb69c1016137`

## Result

Pass. `v0.4.1` is published and installable from the public GitHub tag.
