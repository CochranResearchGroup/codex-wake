# v0.4.14 Release Verification

Date: 2026-05-25

## Scope

Release the P40 OpenClaw Gateway wake transport, repo-linked OpenClaw
`codex-wake` plugin, and transport-selection skill hardening.

## Release Files

- `pyproject.toml` version set to `0.4.14`.
- `docs/releases/v0.4.14.md` added.
- README GitHub install example updated to `v0.4.14`.
- `docs/dev/plans/0041-2026-05-25-v0414-release.md` added.
- `ROADMAP.md` updated with P41.

## Source Validation

Full unit suite:

```text
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

Result:

```text
Ran 112 tests in 0.792s
OK
```

Compile:

```text
python -m compileall -q src tests .codex/hooks
```

Result: pass.

OpenClaw plugin tests:

```text
npm test
```

Result:

```text
tests 9
pass 9
fail 0
```

Node syntax checks:

```text
node --check plugins/openclaw-codex-wake/index.js
node --check plugins/openclaw-codex-wake/lib/scheduler.js
```

Result: pass.

Plugin shell-execution scan:

```text
rg -n "child_process|spawn\(|exec\(|shell" plugins/openclaw-codex-wake
```

Result: no `child_process`, `spawn`, or shell execution. Matches were
documentation text and harmless regex `.exec(...)` calls.

Diff check:

```text
git diff --check
```

Result: pass.

## Build

Build source and wheel distributions:

```text
rm -rf dist build src/codex_wake.egg-info codex_wake.egg-info
python -m venv /tmp/codex-wake-v0414-build
/tmp/codex-wake-v0414-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v0414-build/bin/python -m build
```

Result:

```text
Successfully built codex_wake-0.4.14.tar.gz and codex_wake-0.4.14-py3-none-any.whl
```

## Installed-Wheel Smoke

Install the wheel into a temporary virtual environment:

```text
python -m venv /tmp/codex-wake-v0414-install
/tmp/codex-wake-v0414-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-v0414-install/bin/python -m pip install dist/codex_wake-0.4.14-py3-none-any.whl
```

Result:

```text
Successfully installed codex-wake-0.4.14
```

Verify installed package version:

```text
/tmp/codex-wake-v0414-install/bin/python - <<'PY'
import importlib.metadata
print(importlib.metadata.version("codex-wake"))
PY
```

Result:

```text
0.4.14
```

Verify OpenClaw CLI surfaces and schema:

```text
/tmp/codex-wake-v0414-install/bin/codex-wake openclaw after --help
/tmp/codex-wake-v0414-install/bin/codex-wake openclaw at --help
/tmp/codex-wake-v0414-install/bin/codex-wake schema --json
```

Relevant results:

- `openclaw after` and `openclaw at` expose `--agent`, `--session-key`,
  `--openclaw-path`, channel evidence fields, delivery controls, and
  model/thinking overrides.
- `schema --json` includes `openclaw_gateway` in `target_transports`.
- `schema_version` remains `1`.

Verify installed-wheel OpenClaw target creation:

```text
/tmp/codex-wake-v0414-install/bin/codex-wake \
  --wake-root /tmp/codex-wake-v0414-runtime/wake openclaw after \
  --agent main \
  --session-key agent:main:slack:channel:c0ahqqcg7j4 \
  --workspace default \
  --channel C0AHQQCG7J4 \
  --thread-ts 1779743864.850509 \
  --openclaw-path "$(command -v openclaw)" \
  1m -- "v0.4.14 installed wheel OpenClaw target smoke"
```

Result:

```text
wake_20260526_001327_b086 /tmp/codex-wake-v0414-runtime/wake/pending/wake_20260526_001327_b086.json
```

Resulting target:

```json
{
  "dispatch": {
    "deliver": false,
    "gateway_timeout_ms": 180000,
    "timeout_seconds": 600
  },
  "gateway": {},
  "openclaw": {
    "agent_id": "main",
    "channel": {
      "channel_id": "C0AHQQCG7J4",
      "provider": "slack",
      "thread_ts": "1779743864.850509",
      "workspace": "default"
    },
    "session_key": "agent:main:slack:channel:c0ahqqcg7j4"
  },
  "openclaw_cmd": "/home/ecochran76/.nvm/versions/node/v24.14.0/lib/node_modules/openclaw/openclaw.mjs",
  "transport": "openclaw_gateway"
}
```

Verify placeholder session key rejection from the installed wheel:

```text
/tmp/codex-wake-v0414-install/bin/codex-wake \
  --wake-root /tmp/codex-wake-v0414-runtime/wake openclaw after \
  --agent main --session-key noop-smoke-test 1m -- \
  "invalid placeholder should fail"
```

Result:

```text
codex-wake: session_key contains unsupported placeholder value: noop-smoke-test
```

Exit code: `2`.

## Installed Skill Sync

```text
diff -qr skills/codex-wake /home/ecochran76/.agents/skills/codex-wake
diff -qr skills/codex-wake /home/ecochran76/.codex/shared/skills/codex-wake
diff -qr skills/codex-wake /home/ecochran76/.openclaw/skills/codex-wake
```

Result: all three comparisons passed with no output.

```text
openclaw skills info codex-wake --agent main --json
```

Relevant result:

- `filePath`: `/home/ecochran76/.agents/skills/codex-wake/SKILL.md`
- `eligible`: `true`
- `modelVisible`: `true`
- `userInvocable`: `true`
- `commandVisible`: `true`
- `disabled`: `false`

## OpenClaw Plugin Visibility

```text
openclaw plugins inspect codex-wake --runtime --json
```

Relevant result:

- `status`: `loaded`
- `source`: `/home/ecochran76/workspace.local/codex-wake/plugins/openclaw-codex-wake/index.js`
- `enabled`: `true`
- `activated`: `true`
- `toolNames`: `["codex_wake_schedule"]`
- `commands`: `["codex-wake"]`
- `diagnostics`: `[]`

Gateway catalog:

```text
openclaw gateway call tools.catalog --json \
  --params '{"agentId":"main","includePlugins":true}' | \
  rg 'codex_wake_schedule|codex-wake'
```

Relevant result:

- plugin id `plugin:codex-wake`
- tool id `codex_wake_schedule`

## Compatibility

- Wake record schema remains version `1`.
- `openclaw_gateway` is an additive target transport.
- Tmux dispatch behavior is unchanged.
- Codex app-server dispatch behavior is unchanged.
- The OpenClaw plugin is released as a repo-linked external plugin in this tag;
  it is not installed by the Python `uv tool install` path.

## Known Gaps

- OpenClaw Gateway dispatch requires a real durable OpenClaw session key.
- `codex-waked --no-dispatch` proves only trigger evaluation and state
  movement, not delivery.
- Slack-visible OpenClaw proof requires live channel readback or
  transcript/trajectory evidence after Gateway dispatch.

## Public Tag And Release

Pending release commit, tag push, branch CI, clean public tag install smoke,
GitHub release publication, and user-scoped tool refresh.
