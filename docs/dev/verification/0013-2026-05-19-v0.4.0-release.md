# v0.4.0 Release

Date: 2026-05-19

## Scope

Release the P17 event predicate surface.

## Release Commit And Tag

- Commit: `63ccd12870e7a2f666642aaa3e6b36fee4ad9185`
- Tag: `v0.4.0`
- GitHub release: `https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.4.0`

## Local Source Validation

Commands:

```bash
python -m compileall -q src tests .codex/hooks
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
python -m venv /tmp/codex-wake-v040-build
/tmp/codex-wake-v040-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v040-build/bin/python -m build
python -m venv /tmp/codex-wake-v040-install
/tmp/codex-wake-v040-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-v040-install/bin/python -m pip install dist/*.whl
/tmp/codex-wake-v040-install/bin/codex-wake --help
/tmp/codex-wake-v040-install/bin/codex-waked --once --no-dispatch --wake-root /tmp/codex-wake-v040-empty
```

Outcomes:

- Unit tests: `51` tests passed.
- Built `codex_wake-0.4.0.tar.gz`.
- Built `codex_wake-0.4.0-py3-none-any.whl`.
- Installed wheel reported `codex-wake==0.4.0`.
- Installed-wheel smokes passed for `codex-wake`, `codex-waked`, and `codex-wake-hook`.

## Installed-Wheel Predicate Smoke

From the installed local wheel:

- `codex-wake changed .codex/events/build.log -- ...` created a `file_changed` wake.
- First no-dispatch daemon pass left it pending.
- After modifying the file, the next no-dispatch daemon pass moved it to `firing`.
- `codex-wake pid "$PID" -- ...` created a `process_done` wake.
- First no-dispatch daemon pass left it pending.
- After killing and waiting for the process, the next no-dispatch daemon pass moved it to `firing`.

Final list:

```text
ID                                  STATUS  PREDICATE      NEXT
wake_20260519_212937_3c69           firing  process_done   2026-05-19T21:29:37Z
wake_20260519_212937_da32           firing  file_changed   2026-05-19T21:29:37Z
```

## Public Tag Install Smoke

Commands:

```bash
BASE=/tmp/codex-wake-v040-clean
export UV_TOOL_DIR="$BASE/tools"
export UV_TOOL_BIN_DIR="$BASE/bin"
export UV_CACHE_DIR="$BASE/cache"
export PATH="$BASE/bin:$PATH"
uv tool install --force --reinstall 'git+https://github.com/CochranResearchGroup/codex-wake.git@v0.4.0'
command -v codex-wake
command -v codex-waked
command -v codex-wake-hook
codex-wake --help
codex-waked --once --no-dispatch --wake-root "$BASE/runtime/empty"
printf '{"prompt":"hello","cwd":"%s"}' "$BASE/repo" | codex-wake-hook
```

Predicate smoke from the clean tag install:

```text
checked=1 fired=0 failed=0 pending=1 dispatched=0 submitted=0 requeued=0
checked=1 fired=1 failed=0 pending=0 dispatched=0 submitted=0 requeued=0
checked=1 fired=0 failed=0 pending=1 dispatched=0 submitted=0 requeued=0
checked=1 fired=1 failed=0 pending=0 dispatched=0 submitted=0 requeued=0
ID                                  STATUS  PREDICATE      NEXT
wake_20260519_213013_1a50           firing  file_changed   2026-05-19T21:30:13Z
wake_20260519_213013_9f20           firing  process_done   2026-05-19T21:30:13Z
```

Doctor from the clean tag install reported installed command paths under `/tmp/codex-wake-v040-clean/bin`, service inactive, and the hook trust note.

## Upstream CI

- Run: `26126484154`
- URL: `https://github.com/CochranResearchGroup/codex-wake/actions/runs/26126484154`
- Result: success
- Commit: `63ccd12870e7a2f666642aaa3e6b36fee4ad9185`

## Result

Pass. `v0.4.0` is published and installable from the public GitHub tag.
