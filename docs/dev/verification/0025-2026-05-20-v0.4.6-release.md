# v0.4.6 Release

Date: 2026-05-20

## Scope

Release the P23 `doctor --json` inspection surface.

## Release Commit And Tag

- Commit: `b61670fd997d4ce9730cae794957daa9e044bc59`
- Tag: `v0.4.6`
- GitHub release: `https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.4.6`

## Local Source Validation

Commands:

```bash
python -m compileall -q src tests .codex/hooks
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

Outcome:

- Unit tests: `71` tests passed.
- Compileall passed.

## Installed-Wheel Smoke

Commands:

```bash
python -m venv /tmp/codex-wake-v046-build
/tmp/codex-wake-v046-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v046-build/bin/python -m build --outdir /tmp/codex-wake-v046-dist
python -m venv /tmp/codex-wake-v046-install
/tmp/codex-wake-v046-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-v046-install/bin/python -m pip install /tmp/codex-wake-v046-dist/*.whl
PATH=/tmp/codex-wake-v046-install/bin:$PATH /tmp/codex-wake-v046-install/bin/codex-wake --wake-root /tmp/codex-wake-v046-runtime/wake doctor --repo-root "$PWD"
PATH=/tmp/codex-wake-v046-install/bin:$PATH /tmp/codex-wake-v046-install/bin/codex-wake --wake-root /tmp/codex-wake-v046-runtime/wake doctor --repo-root "$PWD" --json
/tmp/codex-wake-v046-install/bin/python -m pip show codex-wake
```

Outcomes:

- Built `codex_wake-0.4.6.tar.gz`.
- Built `codex_wake-0.4.6-py3-none-any.whl`.
- Installed wheel reported `codex-wake==0.4.6`.
- Text `doctor` preserved existing readiness keys.
- `doctor --json` emitted valid JSON.
- JSON command paths resolved to `/tmp/codex-wake-v046-install/bin/codex-wake`, `/tmp/codex-wake-v046-install/bin/codex-waked`, and `/tmp/codex-wake-v046-install/bin/codex-wake-hook` with the install venv first in `PATH`.

## Public Tag Install Smoke

Commands:

```bash
BASE=/tmp/codex-wake-v046-clean
export UV_TOOL_DIR="$BASE/tools"
export UV_TOOL_BIN_DIR="$BASE/bin"
export UV_CACHE_DIR="$BASE/cache"
export PATH="$BASE/bin:$PATH"
uv tool install --force --reinstall 'git+https://github.com/CochranResearchGroup/codex-wake.git@v0.4.6'
command -v codex-wake
command -v codex-waked
command -v codex-wake-hook
codex-wake --wake-root "$BASE/runtime/wake" doctor --repo-root "$PWD" --json
codex-wake --wake-root "$BASE/runtime/wake" doctor --repo-root "$PWD"
codex-waked --once --no-dispatch --wake-root "$BASE/runtime/empty"
printf '{"prompt":"hello","cwd":"%s"}' "$BASE/repo" | codex-wake-hook
```

Outcomes:

- Public tag resolved to `b61670fd997d4ce9730cae794957daa9e044bc59`.
- Installed `codex-wake==0.4.6`.
- Installed executables: `codex-wake`, `codex-waked`, `codex-wake-hook`.
- Public tag `doctor --json` emitted command paths under `/tmp/codex-wake-v046-clean/bin`.
- Public tag text `doctor` preserved readiness output.
- Daemon one-shot smoke reported no pending work and exited successfully.
- Hook no-wake prompt smoke exited successfully.

## Upstream CI

- Run: `26176601988`
- URL: `https://github.com/CochranResearchGroup/codex-wake/actions/runs/26176601988`
- Result: success
- Commit: `b61670fd997d4ce9730cae794957daa9e044bc59`

## Result

Pass. `v0.4.6` is published and installable from the public GitHub tag.
