# v0.4.4 Release

Date: 2026-05-19

## Scope

Release the P21 hook session visibility diagnostics.

## Release Commit And Tag

- Commit: `a357c0b8c07e760892c0e2dc2c5ca750d0d68803`
- Tag: `v0.4.4`
- GitHub release: `https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.4.4`

## Local Source Validation

Commands:

```bash
python -m compileall -q src tests .codex/hooks
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

Outcome:

- Unit tests: `68` tests passed.
- Compileall passed.

## Installed-Wheel Smoke

Commands:

```bash
python -m venv /tmp/codex-wake-v044-build
/tmp/codex-wake-v044-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v044-build/bin/python -m build
python -m venv /tmp/codex-wake-v044-install
/tmp/codex-wake-v044-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-v044-install/bin/python -m pip install dist/*.whl
/tmp/codex-wake-v044-install/bin/codex-wake --help
/tmp/codex-wake-v044-install/bin/codex-waked --once --no-dispatch --wake-root /tmp/codex-wake-v044-empty
printf '{"prompt":"hello","cwd":"%s"}' "$PWD" | /tmp/codex-wake-v044-install/bin/codex-wake-hook
```

Hook diagnostics smoke:

```bash
ROOT=/tmp/codex-wake-v044-runtime
/tmp/codex-wake-v044-install/bin/codex-wake --wake-root "$ROOT/wake" hook install --repo-root "$ROOT/repo"
/tmp/codex-wake-v044-install/bin/codex-wake --wake-root "$ROOT/wake" hook check --repo-root "$ROOT/repo"
printf '{"wake_id":"wake_seen","submitted_at":"2026-05-19T21:30:00Z","session_id":"session_seen"}\n' > "$ROOT/wake/acks/wake_seen.submitted"
/tmp/codex-wake-v044-install/bin/codex-wake --wake-root "$ROOT/wake" hook check --repo-root "$ROOT/repo"
/tmp/codex-wake-v044-install/bin/codex-wake --wake-root "$ROOT/wake" doctor --repo-root "$ROOT/repo"
```

Outcomes:

- Built `codex_wake-0.4.4.tar.gz`.
- Built `codex_wake-0.4.4-py3-none-any.whl`.
- Installed wheel reported `codex-wake==0.4.4`.
- Installed-wheel smokes passed for `codex-wake`, `codex-waked`, and `codex-wake-hook`.
- Hook diagnostics smoke reported `unknown_without_ack` before an ack existed.
- Hook diagnostics smoke reported `observed_ack` plus latest ack metadata after a submitted ack was written.

## Public Tag Install Smoke

Commands:

```bash
BASE=/tmp/codex-wake-v044-clean
export UV_TOOL_DIR="$BASE/tools"
export UV_TOOL_BIN_DIR="$BASE/bin"
export UV_CACHE_DIR="$BASE/cache"
export PATH="$BASE/bin:$PATH"
uv tool install --force --reinstall 'git+https://github.com/CochranResearchGroup/codex-wake.git@v0.4.4'
command -v codex-wake
command -v codex-waked
command -v codex-wake-hook
codex-wake --help
codex-waked --once --no-dispatch --wake-root "$BASE/runtime/empty"
printf '{"prompt":"hello","cwd":"%s"}' "$BASE/repo" | codex-wake-hook
```

Hook diagnostics smoke from the clean tag install:

```text
hook_ack_count=1
hook_active_session_loaded=observed_ack
hook_latest_ack_path=/tmp/codex-wake-v044-clean/runtime/wake/acks/wake_seen.submitted
hook_latest_ack_submitted_at=2026-05-19T21:30:00Z
hook_latest_ack_wake_id=wake_seen
hook_latest_ack_session_id=session_seen
```

Outcomes:

- Public tag resolved to `a357c0b8c07e760892c0e2dc2c5ca750d0d68803`.
- Installed `codex-wake==0.4.4`.
- Installed executables: `codex-wake`, `codex-waked`, `codex-wake-hook`.

## Upstream CI

- Run: `26137262586`
- URL: `https://github.com/CochranResearchGroup/codex-wake/actions/runs/26137262586`
- Result: success
- Commit: `a357c0b8c07e760892c0e2dc2c5ca750d0d68803`

## Result

Pass. `v0.4.4` is published and installable from the public GitHub tag.
