# v0.4.11 Release Verification

Date: 2026-05-21

## Scope

Release the P33 app-server target discovery CLI change.

## Release Files

- `pyproject.toml` version set to `0.4.11`.
- `docs/releases/v0.4.11.md` added.

## Source Validation

Compile:

```text
PYTHONPATH=src python -m compileall -q src tests
```

Result: pass.

Full unit suite:

```text
PYTHONPATH=src python -m unittest discover -s tests
```

Result:

```text
Ran 83 tests in 0.643s
OK
```

Diff check:

```text
git diff --check
```

Result: pass.

## Build

Build source and wheel distributions:

```text
python -m venv /tmp/codex-wake-v0411-build
/tmp/codex-wake-v0411-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v0411-build/bin/python -m build
```

Result:

```text
Successfully built codex_wake-0.4.11.tar.gz and codex_wake-0.4.11-py3-none-any.whl
```

## Installed-Wheel Smoke

Install the wheel into a temporary virtual environment:

```text
python -m venv /tmp/codex-wake-v0411-install
/tmp/codex-wake-v0411-install/bin/python -m pip install dist/codex_wake-0.4.11-py3-none-any.whl
```

Result:

```text
Successfully installed codex-wake-0.4.11
```

Verify installed package version:

```text
/tmp/codex-wake-v0411-install/bin/python - <<'PY'
import importlib.metadata
print(importlib.metadata.version("codex-wake"))
PY
```

Result:

```text
0.4.11
```

Verify app-server candidates help:

```text
/tmp/codex-wake-v0411-install/bin/codex-wake app candidates --help
```

Result included:

```text
usage: codex-wake app candidates [-h] [--codex-home CODEX_HOME] [--cwd CWD]
                                 [--limit LIMIT] [--json]
```

Verify app-server candidates JSON smoke:

```text
/tmp/codex-wake-v0411-install/bin/codex-wake app candidates --cwd "$PWD" --json --limit 2
```

Result: emitted JSON rows for local session rollouts whose `cwd` matched this repo, with `resumable_source` set to `local_session_rollout`.

## Compatibility

- Wake record schema remains version `1`.
- App-server wake dispatch behavior is unchanged.
- Tmux dispatch behavior is unchanged.
- Only `stdio://` app-server dispatch is implemented.

## Known Gaps

- Candidate discovery identifies local rollout-backed thread ids; it does not prove the thread is currently idle.

## Public Tag And Release

Pushed release commit and tag:

```text
git push origin main
git push origin v0.4.11
```

Branch CI:

- Run: `https://github.com/CochranResearchGroup/codex-wake/actions/runs/26201481308`
- Result: success on Python 3.11 and Python 3.12.

Clean public tag install smoke:

```text
python -m venv /tmp/codex-wake-v0411-public
/tmp/codex-wake-v0411-public/bin/python -m pip install 'git+https://github.com/CochranResearchGroup/codex-wake.git@v0.4.11'
/tmp/codex-wake-v0411-public/bin/python - <<'PY'
import importlib.metadata
print(importlib.metadata.version("codex-wake"))
PY
/tmp/codex-wake-v0411-public/bin/codex-wake app candidates --help
/tmp/codex-wake-v0411-public/bin/codex-wake app candidates --cwd "$PWD" --json --limit 2
```

Result:

```text
Successfully installed codex-wake-0.4.11
0.4.11
usage: codex-wake app candidates [-h] [--codex-home CODEX_HOME] [--cwd CWD]
                                 [--limit LIMIT] [--json]
```

The repo-filtered JSON smoke emitted local session rollout rows with `resumable_source` set to `local_session_rollout`.

GitHub release:

- `https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.4.11`
