# v0.5.0 Release Verification

Date: 2026-05-26

Plan: `docs/dev/plans/0043-2026-05-26-productization-completion.md`

## Scope

Record the P43 productization release closeout for `v0.5.0`.

## Commit, Tag, Release

Release commit:

```text
64627ab7217c4eab232127ed3d388d46964b9e39
```

Tag:

```text
v0.5.0 -> 64627ab7217c4eab232127ed3d388d46964b9e39
```

GitHub release:

```text
https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.5.0
publishedAt=2026-05-26T13:12:04Z
draft=false
prerelease=false
```

CI:

```text
run_id=26450020574
url=https://github.com/CochranResearchGroup/codex-wake/actions/runs/26450020574
headSha=64627ab7217c4eab232127ed3d388d46964b9e39
status=completed
conclusion=success
```

CI passed release gates on Python 3.11 and 3.12:

- compile sources
- run unit tests
- run OpenClaw plugin tests
- check OpenClaw plugin entrypoints
- build package
- smoke installed wheel through `scripts/product_smoke.py`

## Local Release Validation

Before commit/tag:

```bash
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
# Ran 155 tests - OK

python -m compileall -q src tests .codex/hooks
python -m py_compile scripts/product_smoke.py

npm --prefix plugins/openclaw-codex-wake test
# tests 12, pass 12

node --check plugins/openclaw-codex-wake/index.js
node --check plugins/openclaw-codex-wake/lib/scheduler.js

git diff --check
```

The OpenClaw plugin no-shell scan passed:

```text
no unsafe child_process imports or spawn/execFile calls found
```

Package build:

```text
dist/codex_wake-0.5.0.tar.gz
dist/codex_wake-0.5.0-py3-none-any.whl
```

Installed-wheel product smoke:

```text
cli_version=0.5.0
schema_version=1
supervisor_once_roots=1
```

Artifact:

```text
.codex/wake/smoke/0070-v050-installed-wheel
```

## Public Tag Smoke

Public tag install smoke:

```bash
python scripts/product_smoke.py --public-tag v0.5.0 --artifact-dir .codex/wake/smoke/0071-public-tag-v050 --json
```

Result:

```text
public_tag=v0.5.0
cli_version=0.5.0
schema_version=1
supervisor_once_roots=1
tmux_status=manual_only
```

## User-Scoped Install Refresh

User install was refreshed from the public tag:

```bash
uv tool install --force --reinstall git+https://github.com/CochranResearchGroup/codex-wake.git@v0.5.0
```

Result:

```text
codex-wake v0.5.0
- codex-wake
- codex-wake-hook
- codex-waked
```

Installed command:

```text
codex-wake 0.5.0
```

Supervisor after restart:

```text
supervisor_active=active
root=codex-wake-98975473 health_status=ready recent=true
monitor_ready=true
monitor_source=supervisor
```

## OpenClaw Plugin Runtime

The OpenClaw plugin was updated from the public `v0.5.0` tag:

```text
plugin_version=0.5.0
package_version=0.5.0
source_path=/home/ecochran76/.local/share/codex-wake/openclaw-plugins/v0.5.0
installed=true
```

After Gateway restart:

```text
gateway_rpc_ok=true
gateway_url=ws://127.0.0.1:18789
plugin_version=0.5.0
plugin_source=/home/ecochran76/.openclaw/extensions/codex-wake/index.js
plugin_activated=true
tool_names=codex_wake_schedule
diagnostics=0
```

Installed product readiness:

```text
readiness=warning
readiness_cli_version=0.5.0
app_server=ready
cli=ready
hooks=warning
monitor=ready
openclaw_gateway=ready
openclaw_plugin=ready
repo_service=warning
skills=ready
supervisor=ready
tmux=ready
```

Warnings are expected for this workstation: both project and user hook sources
are installed, and the user supervisor owns the root while the repo-scoped
service is inactive.

## Live Smoke Evidence

Fresh live product smokes are recorded in:

```text
docs/dev/verification/0068-2026-05-26-live-product-smokes.md
```

Summary:

```text
Codex app-server wake: wake_20260526_130227_30c0
OpenClaw Gateway wake: wake_20260526_130335_d7b5
OpenClaw Slack marker readback: CODEX_WAKE_PRODUCT_SMOKE_OPENCLAW_20260526T130335Z
Final wake root: active_total=0, terminal_total=0, archived_total=23
```

## Closeout

P43 DOD is satisfied:

- OpenClaw plugin install/update works from a durable public tag and no longer
  depends on a repo-linked path.
- Installed readiness is available from `codex-wake product-readiness --json`.
- State cleanup and supervisor root lifecycle are documented and validated.
- The product smoke matrix covers installed CLI, supervisor, Codex app-server,
  OpenClaw Gateway, and manual/operator-visible tmux boundaries.
- Operator docs and the `codex-wake` skill describe the productized path.
- `v0.5.0` is tagged, published, public-install-smoked, and installed
  user-scoped from the public tag.
