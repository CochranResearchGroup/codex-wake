# v0.4.7 Release

Date: 2026-05-20

## Scope

Release the P24 `status` and `status --json` summary surface.

## Release Commit And Tag

- Commit: `5aa3f61211a86ce4dc967ac6d76f25cdbe676bb7`
- Tag: `v0.4.7`
- GitHub release: `https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.4.7`

## Local Source Validation

Commands:

```bash
python -m compileall -q src tests .codex/hooks
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

Outcome:

- Unit tests: `73` tests passed.
- Compileall passed.

## Installed-Wheel Smoke

Commands:

```bash
python -m venv /tmp/codex-wake-v047-build
/tmp/codex-wake-v047-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v047-build/bin/python -m build --outdir /tmp/codex-wake-v047-dist
python -m venv /tmp/codex-wake-v047-install
/tmp/codex-wake-v047-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-v047-install/bin/python -m pip install /tmp/codex-wake-v047-dist/*.whl
PATH=/tmp/codex-wake-v047-install/bin:$PATH /tmp/codex-wake-v047-install/bin/codex-wake --wake-root /tmp/codex-wake-v047-runtime/wake after --app-server-thread-id thread_status 1m -- 'Release status smoke'
PATH=/tmp/codex-wake-v047-install/bin:$PATH /tmp/codex-wake-v047-install/bin/codex-wake --wake-root /tmp/codex-wake-v047-runtime/wake status
PATH=/tmp/codex-wake-v047-install/bin:$PATH /tmp/codex-wake-v047-install/bin/codex-wake --wake-root /tmp/codex-wake-v047-runtime/wake status --json
/tmp/codex-wake-v047-install/bin/python -m pip show codex-wake
```

Outcomes:

- Built `codex_wake-0.4.7.tar.gz`.
- Built `codex_wake-0.4.7-py3-none-any.whl`.
- Installed wheel reported `codex-wake==0.4.7`.
- Installed CLI created an app-server-targeted pending wake.
- Installed `status` reported `total=1`, `active_total=1`, and `counts_by_target_transport=app-server:1`.
- Installed `status --json` emitted valid JSON with the same counts.

## Public Tag Install Smoke

Commands:

```bash
BASE=/tmp/codex-wake-v047-clean
export UV_TOOL_DIR="$BASE/tools"
export UV_TOOL_BIN_DIR="$BASE/bin"
export UV_CACHE_DIR="$BASE/cache"
export PATH="$BASE/bin:$PATH"
uv tool install --force --reinstall 'git+https://github.com/CochranResearchGroup/codex-wake.git@v0.4.7'
command -v codex-wake
command -v codex-waked
command -v codex-wake-hook
codex-wake --wake-root "$BASE/runtime/wake" after --app-server-thread-id thread_status 1m -- 'Public status smoke'
codex-wake --wake-root "$BASE/runtime/wake" status
codex-wake --wake-root "$BASE/runtime/wake" status --json
codex-waked --once --no-dispatch --wake-root "$BASE/runtime/empty"
printf '{"prompt":"hello","cwd":"%s"}' "$BASE/repo" | codex-wake-hook
```

Outcomes:

- Public tag resolved to `5aa3f61211a86ce4dc967ac6d76f25cdbe676bb7`.
- Installed `codex-wake==0.4.7`.
- Installed executables: `codex-wake`, `codex-waked`, `codex-wake-hook`.
- Public tag CLI created an app-server-targeted pending wake.
- Public tag `status` reported `total=1`, `active_total=1`, and `counts_by_target_transport=app-server:1`.
- Public tag `status --json` emitted valid JSON with the same counts.
- Daemon one-shot smoke reported no pending work for the empty root and exited successfully.
- Hook no-wake prompt smoke exited successfully.

## Upstream CI

- Run: `26179270632`
- URL: `https://github.com/CochranResearchGroup/codex-wake/actions/runs/26179270632`
- Result: success
- Commit: `5aa3f61211a86ce4dc967ac6d76f25cdbe676bb7`

## Result

Pass. `v0.4.7` is published and installable from the public GitHub tag.
