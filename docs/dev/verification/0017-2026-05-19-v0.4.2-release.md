# v0.4.2 Release

Date: 2026-05-19

## Scope

Release the P19 PID reuse hardening surface.

## Release Commit And Tag

- Commit: `e6c91ddad3129b61eccedf3195a5578558062c73`
- Tag: `v0.4.2`
- GitHub release: `https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.4.2`

## Local Source Validation

Commands:

```bash
python -m compileall -q src tests .codex/hooks
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

Outcome:

- Unit tests: `61` tests passed.
- Compileall passed.

## Installed-Wheel Smoke

Commands:

```bash
python -m venv /tmp/codex-wake-v042-build
/tmp/codex-wake-v042-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v042-build/bin/python -m build
python -m venv /tmp/codex-wake-v042-install
/tmp/codex-wake-v042-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-v042-install/bin/python -m pip install dist/*.whl
/tmp/codex-wake-v042-install/bin/codex-wake --help
/tmp/codex-wake-v042-install/bin/codex-waked --once --no-dispatch --wake-root /tmp/codex-wake-v042-empty
printf '{"prompt":"hello","cwd":"%s"}' "$PWD" | /tmp/codex-wake-v042-install/bin/codex-wake-hook
```

PID smoke:

```bash
ROOT=/tmp/codex-wake-v042-runtime
sleep 30 &
PID=$!
/tmp/codex-wake-v042-install/bin/codex-wake --wake-root "$ROOT/wake" pid "$PID" -- 'Release 0.4.2 process done smoke'
/tmp/codex-wake-v042-install/bin/codex-wake --wake-root "$ROOT/wake" list --json
/tmp/codex-wake-v042-install/bin/codex-waked --wake-root "$ROOT/wake" --once --no-dispatch
kill "$PID"
wait "$PID" 2>/dev/null || true
/tmp/codex-wake-v042-install/bin/codex-waked --wake-root "$ROOT/wake" --once --no-dispatch
/tmp/codex-wake-v042-install/bin/codex-wake --wake-root "$ROOT/wake" list
```

Outcomes:

- Built `codex_wake-0.4.2.tar.gz`.
- Built `codex_wake-0.4.2-py3-none-any.whl`.
- Installed wheel reported `codex-wake==0.4.2`.
- Installed-wheel smokes passed for `codex-wake`, `codex-waked`, and `codex-wake-hook`.
- PID smoke recorded `registered_start_time_ticks` and `registered_boot_id` on this host.
- First no-dispatch daemon pass kept the wake pending while the process lived.
- Second no-dispatch daemon pass moved the wake to `firing` after process exit.

## Public Tag Install Smoke

Commands:

```bash
BASE=/tmp/codex-wake-v042-clean
export UV_TOOL_DIR="$BASE/tools"
export UV_TOOL_BIN_DIR="$BASE/bin"
export UV_CACHE_DIR="$BASE/cache"
export PATH="$BASE/bin:$PATH"
uv tool install --force --reinstall 'git+https://github.com/CochranResearchGroup/codex-wake.git@v0.4.2'
command -v codex-wake
command -v codex-waked
command -v codex-wake-hook
codex-wake --help
codex-waked --once --no-dispatch --wake-root "$BASE/runtime/empty"
printf '{"prompt":"hello","cwd":"%s"}' "$BASE/repo" | codex-wake-hook
```

PID smoke from the clean tag install:

```text
clean_identity_recorded=true
clean_boot_id_recorded=true
checked=1 fired=0 failed=0 pending=1 dispatched=0 submitted=0 requeued=0
checked=1 fired=1 failed=0 pending=0 dispatched=0 submitted=0 requeued=0
wake_20260519_230425_cbe6 firing process_done
```

Outcomes:

- Public tag resolved to `e6c91ddad3129b61eccedf3195a5578558062c73`.
- Installed `codex-wake==0.4.2`.
- Installed executables: `codex-wake`, `codex-waked`, `codex-wake-hook`.

## Upstream CI

- Run: `26130628026`
- URL: `https://github.com/CochranResearchGroup/codex-wake/actions/runs/26130628026`
- Result: success
- Commit: `e6c91ddad3129b61eccedf3195a5578558062c73`

## Result

Pass. `v0.4.2` is published and installable from the public GitHub tag.
