# v0.4.9 Release Verification

Date: 2026-05-21

## Scope

Release the P30 app-server thread-status preflight CLI/runtime change.

## Release Files

- `pyproject.toml` version set to `0.4.9`.
- `docs/releases/v0.4.9.md` added.

## Source Validation

Compile:

```text
PYTHONPATH=src python -m compileall -q src tests
```

Result: pass.

Full unit suite:

```text
PYTHONPATH=src python -m unittest discover -s tests
```

Result:

```text
Ran 77 tests in 0.812s
OK
```

Diff check:

```text
git diff --check
```

Result: pass.

## Build

Built source and wheel distributions:

```text
python -m venv /tmp/codex-wake-v049-build
/tmp/codex-wake-v049-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v049-build/bin/python -m build
```

Result:

```text
Successfully built codex_wake-0.4.9.tar.gz and codex_wake-0.4.9-py3-none-any.whl
```

## Installed-Wheel Smoke

Installed the wheel into a temporary virtual environment:

```text
python -m venv /tmp/codex-wake-v049-install
/tmp/codex-wake-v049-install/bin/python -m pip install dist/codex_wake-0.4.9-py3-none-any.whl
```

Result:

```text
Successfully installed codex-wake-0.4.9
```

Verified app-server status help:

```text
/tmp/codex-wake-v049-install/bin/codex-wake app status --help
```

Result included:

```text
usage: codex-wake app status [-h] [--endpoint ENDPOINT] [--json] thread_id
```

Verified installed app-server-targeted wake creation:

```text
/tmp/codex-wake-v049-install/bin/codex-wake --wake-root "$ROOT/wake" app after thread_abc 1m -- 'Release 0.4.9 app wake smoke'
/tmp/codex-wake-v049-install/bin/codex-wake --wake-root "$ROOT/wake" list --json
```

Result: created a pending app-server-targeted wake with:

```json
{
  "target": {
    "endpoint": "stdio://",
    "thread_id": "thread_abc",
    "transport": "app-server"
  }
}
```

## Compatibility

- Wake record schema remains version `1`.
- App-server preflight metadata is additive.
- Tmux dispatch behavior is unchanged.
- Only `stdio://` app-server dispatch is implemented.

## Known Gaps

- Live app-server `turn/start` dogfood is still deferred.
- Public tag install smoke and GitHub release publication completed after tag push.

## Public Tag And Release

Pushed release commit and tag:

```text
git push origin main
git push origin v0.4.9
```

Branch CI:

- Run: `https://github.com/CochranResearchGroup/codex-wake/actions/runs/26199151140`
- Result: success on Python 3.11 and Python 3.12.

Clean public tag install smoke:

```text
python -m venv /tmp/codex-wake-v049-public
/tmp/codex-wake-v049-public/bin/python -m pip install 'git+https://github.com/CochranResearchGroup/codex-wake.git@v0.4.9'
/tmp/codex-wake-v049-public/bin/python - <<'PY'
import importlib.metadata
print(importlib.metadata.version('codex-wake'))
PY
/tmp/codex-wake-v049-public/bin/codex-wake app status --help
```

Result:

```text
Successfully installed codex-wake-0.4.9
0.4.9
usage: codex-wake app status [-h] [--endpoint ENDPOINT] [--json] thread_id
```

Public tag app wake smoke:

```text
/tmp/codex-wake-v049-public/bin/codex-wake --wake-root "$ROOT/wake" app after thread_abc 1m -- 'Public tag app wake smoke'
/tmp/codex-wake-v049-public/bin/codex-wake --wake-root "$ROOT/wake" list --json
```

Result: created a pending app-server-targeted wake with `transport` set to `app-server`.

GitHub release:

- `https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.4.9`
