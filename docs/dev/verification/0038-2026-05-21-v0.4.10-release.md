# v0.4.10 Release Verification

Date: 2026-05-21

## Scope

Release the P32 resume-backed app-server status CLI change.

## Release Files

- `pyproject.toml` version set to `0.4.10`.
- `docs/releases/v0.4.10.md` added.

## Source Validation

Compile:

```text
PYTHONPATH=src python -m compileall -q src tests
```

Result: pass.

Full unit suite:

```text
PYTHONPATH=src python -m unittest discover -s tests
```

Result:

```text
Ran 79 tests in 0.717s
OK
```

Diff check:

```text
git diff --check
```

Result: pass.

## Build

Build source and wheel distributions:

```text
python -m venv /tmp/codex-wake-v0410-build
/tmp/codex-wake-v0410-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v0410-build/bin/python -m build
```

Result:

```text
Successfully built codex_wake-0.4.10.tar.gz and codex_wake-0.4.10-py3-none-any.whl
```

## Installed-Wheel Smoke

Install the wheel into a temporary virtual environment:

```text
python -m venv /tmp/codex-wake-v0410-install
/tmp/codex-wake-v0410-install/bin/python -m pip install dist/codex_wake-0.4.10-py3-none-any.whl
```

Result:

```text
Successfully installed codex-wake-0.4.10
```

Verify installed package version:

```text
/tmp/codex-wake-v0410-install/bin/python - <<'PY'
import importlib.metadata
print(importlib.metadata.version("codex-wake"))
PY
```

Result:

```text
0.4.10
```

Verify app-server status help:

```text
/tmp/codex-wake-v0410-install/bin/codex-wake app status --help
```

Result included:

```text
usage: codex-wake app status [-h] [--endpoint ENDPOINT] [--json] [--resume]
                             thread_id
  --resume             resume the thread before reading status; does not start
                       a turn
```

## Compatibility

- Wake record schema remains version `1`.
- App-server wake dispatch behavior is unchanged.
- Tmux dispatch behavior is unchanged.
- Only `stdio://` app-server dispatch is implemented.

## Known Gaps

- Public tag install smoke and GitHub release publication are pending tag push.
