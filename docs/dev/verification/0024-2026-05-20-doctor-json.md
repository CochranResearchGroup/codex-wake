# Doctor JSON Inspection Surface

Date: 2026-05-20

Lane: P23

## Scope

Add `codex-wake doctor --json` for automation while preserving existing text doctor output.

## Changes

- Added `doctor --json`.
- Factored doctor collection into one structured summary used by text and JSON output.
- Preserved existing text output keys for operator compatibility.
- Added JSON fields for repo root, wake root, command paths, tmux path, hook config, hook runtime evidence, service status, service paths, and trust note.
- Updated README usage.

## Source Validation

Commands:

```bash
PYTHONPATH=src python -m unittest tests.test_cli.CliTests.test_doctor_reports_readiness_fields tests.test_cli.CliTests.test_doctor_json_reports_readiness_fields
PYTHONPATH=src python -m codex_wake.cli --wake-root /tmp/codex-wake-p23-smoke/wake doctor --repo-root /home/ecochran76/workspace.local/codex-wake --json
python -m compileall -q src tests .codex/hooks
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

Outcomes:

- Focused doctor tests passed.
- Source CLI `doctor --json` emitted valid JSON.
- Compileall passed.
- Unit tests: `71` tests passed.

## Installed-Wheel Smoke

Commands:

```bash
python -m venv /tmp/codex-wake-p23-build
/tmp/codex-wake-p23-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-p23-build/bin/python -m build --outdir /tmp/codex-wake-p23-dist
python -m venv /tmp/codex-wake-p23-install
/tmp/codex-wake-p23-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-p23-install/bin/python -m pip install /tmp/codex-wake-p23-dist/*.whl
/tmp/codex-wake-p23-install/bin/codex-wake --wake-root /tmp/codex-wake-p23-runtime/wake doctor --repo-root "$PWD"
/tmp/codex-wake-p23-install/bin/codex-wake --wake-root /tmp/codex-wake-p23-runtime/wake doctor --repo-root "$PWD" --json
PATH=/tmp/codex-wake-p23-install/bin:$PATH /tmp/codex-wake-p23-install/bin/codex-wake --wake-root /tmp/codex-wake-p23-runtime-pathfirst/wake doctor --repo-root "$PWD" --json
```

Outcomes:

- Built and installed a local working-tree wheel.
- Installed text `doctor` preserved existing key-value diagnostics.
- Installed `doctor --json` emitted valid structured JSON.
- With the installed venv first in `PATH`, JSON command fields pointed at `/tmp/codex-wake-p23-install/bin/codex-wake`, `/tmp/codex-wake-p23-install/bin/codex-waked`, and `/tmp/codex-wake-p23-install/bin/codex-wake-hook`.
- JSON reported hook config installed for this repo and `hook_runtime.active_session_loaded=unknown_without_ack` for the isolated wake root.

## Known Gaps

- This lane adds an inspection surface only; it does not add a live health API or change service behavior.
- JSON command path fields reflect `PATH` resolution from the calling environment.

## Result

Pass. P23 is closed.
