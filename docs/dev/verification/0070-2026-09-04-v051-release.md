# v0.5.1 Release Verification

Date: 2026-09-04

Plan: `docs/dev/plans/0046-2026-09-04-v051-maintenance-release.md`

## Scope

Record git reconciliation, validation, publication, public-tag smoke, and
installed runtime readback for the P46 `v0.5.1` maintenance release.

## Git Maintenance

After `git fetch --prune origin`, `origin/main` was not ahead and local `main`
was eleven commits ahead. The existing history was preserved. P44/P45 and the
release metadata were added as two coherent commits, then the complete range
was pushed without rebasing, squashing, or force-pushing:

```text
626baf4..c31a03b  main -> main
82b1e4a Harden app-server and cross-root wake handling
c31a03b Prepare v0.5.1 maintenance release
```

## Commit, Tag, Release

Release commit and annotated tag target:

```text
c31a03b7fce77679a08ddf9eb7036f545f055d85
v0.5.1 -> c31a03b7fce77679a08ddf9eb7036f545f055d85
```

GitHub release:

```text
https://github.com/CochranResearchGroup/codex-wake/releases/tag/v0.5.1
publishedAt=2026-09-04T09:33:06Z
draft=false
prerelease=false
```

CI:

```text
run_id=33858855558
url=https://github.com/CochranResearchGroup/codex-wake/actions/runs/33858855558
headSha=c31a03b7fce77679a08ddf9eb7036f545f055d85
status=completed
conclusion=success
```

Both Python 3.11 and 3.12 release-gate jobs passed compilation, unit tests,
OpenClaw plugin tests and syntax checks, package build, and installed-wheel
smoke.

## Local Release Validation

```text
Python unit tests: 180 passed
OpenClaw plugin tests: 12 passed
Python compileall and product-smoke py_compile: passed
OpenClaw plugin entrypoint syntax: passed
Active planning audit: passed
git diff --check: passed
```

Built package artifacts:

```text
codex_wake-0.5.1-py3-none-any.whl
sha256=6e9bc30d2a1bb9623ad0a40257027bfa526358ee0afbb41507acad6569b0a998
codex_wake-0.5.1.tar.gz
sha256=6bd3322035735eaeaf0ee99fd240c914e6bdab5c8e4ec2a75453e373a34dc0c3
```

The isolated installed-wheel smoke reported:

```text
cli_version=0.5.1
schema_version=1
supervisor_once_roots=1
tmux_status=manual_only
```

## Public Tag And User Install

Public-tag smoke against `v0.5.1` reported:

```text
public_tag=v0.5.1
cli_version=0.5.1
schema_version=1
supervisor_once_roots=1
tmux_status=manual_only
```

The user-scoped CLI was refreshed directly from the public tag:

```text
codex-wake 0.5.1
/home/ecochran76/.local/bin/codex-wake
/home/ecochran76/.local/bin/codex-wake-hook
/home/ecochran76/.local/bin/codex-waked
```

The user hook is valid and installed at
`/home/ecochran76/.codex/hooks.json`. The project hook remains absent, so only
one hook source is installed for this repository.

Tracked skill content was synchronized and compared successfully with:

```text
/home/ecochran76/.agents/skills/codex-wake
/home/ecochran76/.codex/shared/skills/codex-wake
/home/ecochran76/.openclaw/skills/codex-wake
```

OpenClaw reports the selected skill as eligible and model-visible from
`/home/ecochran76/.agents/skills/codex-wake/SKILL.md`.

## Installed Runtime Behavior

A disposable installed-CLI app-server record persisted:

```text
transport=app-server
retry_active_writer=true
```

A second disposable wake was created, cancelled, archived under one root, and
submitted to the installed hook from a different cwd with its explicit owner
root. The hook returned:

```text
status=archived
previous_status=cancelled
Do not resume the scheduled task from this prompt.
owner_ack=true
receiver_ack=false
```

This confirms the public install carries the P44 option and P45 owner-root,
archive, terminal-context, and acknowledgment behavior.

## Supervisor And OpenClaw Plugin

The reinstalled user supervisor is enabled and active. Its three enrolled
roots report `health_status=ready` and `health_recent=true`.

The OpenClaw plugin was refreshed from public tag `v0.5.1`, followed by a
Gateway restart. Runtime inspection reports:

```text
version=0.5.1
source=/home/ecochran76/.openclaw/extensions/codex-wake/index.js
activated=true
tool_names=codex_wake_schedule
diagnostics=0
```

Installed product readiness reports all functional surfaces ready:

```text
app_server=ready
cli=ready
hooks=ready
monitor=ready
openclaw_gateway=ready
openclaw_plugin=ready
skills=ready
supervisor=ready
tmux=ready
repo_service=warning
```

The overall warning is expected: the repo-scoped service is intentionally
inactive while the enabled user supervisor owns this root.

## Validation Boundary

No live app-server, tmux injection, or OpenClaw message delivery was initiated.
Those paths require real target/session identity and can produce visible
external effects. Existing tests, installed disposable-record replay, public
tag smoke, Gateway/plugin readback, and supervisor health cover this
maintenance release without an unsolicited delivery.

## Closeout

P46 is complete: `v0.5.1` is pushed, tagged, CI-passed, published,
public-install-smoked, installed user-scoped, and verified across the hook,
skills, supervisor, and OpenClaw plugin.
