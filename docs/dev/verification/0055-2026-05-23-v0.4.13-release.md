# v0.4.13 Release Verification

Date: 2026-05-23

## Scope

Release the P38 app-server service environment product hardening.

## Release Files

- `pyproject.toml` version set to `0.4.13`.
- `docs/releases/v0.4.13.md` added.
- README GitHub install example updated to `v0.4.13`.

## Source Validation

Full unit suite:

```text
PYTHONPATH=src python -m unittest discover -s tests -p 'test_*.py'
```

Result:

```text
Ran 100 tests in 0.846s
OK
```

Compile:

```text
python -m compileall -q src tests .codex/hooks
```

Result: pass.

Diff check:

```text
git diff --check
```

Result: pass.

## Build

Build source and wheel distributions:

```text
rm -rf dist build src/codex_wake.egg-info codex_wake.egg-info
python -m venv /tmp/codex-wake-v0413-build
/tmp/codex-wake-v0413-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v0413-build/bin/python -m build
```

Result:

```text
Successfully built codex_wake-0.4.13.tar.gz and codex_wake-0.4.13-py3-none-any.whl
```

## Installed-Wheel Smoke

Install the wheel into a temporary virtual environment:

```text
python -m venv /tmp/codex-wake-v0413-install
/tmp/codex-wake-v0413-install/bin/python -m pip install --upgrade pip
/tmp/codex-wake-v0413-install/bin/python -m pip install dist/codex_wake-0.4.13-py3-none-any.whl
```

Result:

```text
Successfully installed codex-wake-0.4.13
```

Verify installed package version:

```text
/tmp/codex-wake-v0413-install/bin/python - <<'PY'
import importlib.metadata
print(importlib.metadata.version("codex-wake"))
PY
```

Result:

```text
0.4.13
```

Verify new installed CLI surfaces:

```text
/tmp/codex-wake-v0413-install/bin/codex-wake app after --help
/tmp/codex-wake-v0413-install/bin/codex-wake service install --help
```

Result: both help outputs included `--codex-path`.

Verify service unit environment rendering from the installed wheel:

```text
XDG_CONFIG_HOME="$TMP/config" XDG_STATE_HOME="$TMP/state" \
/tmp/codex-wake-v0413-install/bin/codex-wake --wake-root "$TMP/wake" \
  service install --no-start --name codex-wake-v0413-wheel \
  --repo-root "$PWD" --daemon-path /tmp/codex-wake-v0413-install/bin/codex-waked \
  --codex-path "$(command -v codex)" --log-path "$TMP/state/service.log"
```

Rendered unit included:

```text
Environment="CODEX_WAKE_CODEX_CMD=/home/ecochran76/.nvm/versions/node/v24.14.0/lib/node_modules/@openai/codex/bin/codex.js"
ExecStart="/tmp/codex-wake-v0413-install/bin/codex-waked" --wake-root ".../wake" --interval 1
```

Verify app-server target command persistence from the installed wheel:

```text
/tmp/codex-wake-v0413-install/bin/codex-wake --wake-root "$TMP/wake" \
  app after --codex-path "$(command -v codex)" thread_v0413 1m -- \
  "v0.4.13 wheel target smoke"
```

Resulting target:

```json
{
  "codex_cmd": "/home/ecochran76/.nvm/versions/node/v24.14.0/lib/node_modules/@openai/codex/bin/codex.js",
  "endpoint": "stdio://",
  "thread_id": "thread_v0413",
  "transport": "app-server"
}
```

## Compatibility

- Wake record schema remains version `1`.
- `target.codex_cmd` is optional and additive.
- Tmux dispatch behavior is unchanged.
- `stdio://` remains the only implemented app-server endpoint for Codex Wake dispatch.

## Known Gaps

- `doctor` reports command-resolution readiness, not proof that a specific
  future app-server thread will remain idle.
- App-server dispatch can resume a thread without showing a visible turn in a
  live tmux pane the operator is watching.

## Public Tag And Release

Pending release commit, tag push, branch CI, clean public tag install smoke, and
GitHub release publication.
