# v0.4.12 Release Verification

Date: 2026-05-21

## Scope

Release the P34 validated app-server candidate discovery CLI change.

## Release Files

- `pyproject.toml` version set to `0.4.12`.
- `docs/releases/v0.4.12.md` added.

## Source Validation

Compile:

```text
PYTHONPATH=src python -m compileall -q src tests
```

Result: pass.

Full unit suite:

```text
PYTHONPATH=src python -m unittest discover -s tests
```

Result:

```text
Ran 86 tests in 0.663s
OK
```

Diff check:

```text
git diff --check
```

Result: pass.

## Build

Build source and wheel distributions:

```text
python -m venv /tmp/codex-wake-v0412-build
/tmp/codex-wake-v0412-build/bin/python -m pip install --upgrade pip build
/tmp/codex-wake-v0412-build/bin/python -m build
```

Result:

```text
Successfully built codex_wake-0.4.12.tar.gz and codex_wake-0.4.12-py3-none-any.whl
```

## Installed-Wheel Smoke

Install the wheel into a temporary virtual environment:

```text
python -m venv /tmp/codex-wake-v0412-install
/tmp/codex-wake-v0412-install/bin/python -m pip install dist/codex_wake-0.4.12-py3-none-any.whl
```

Result:

```text
Successfully installed codex-wake-0.4.12
```

Verify installed package version:

```text
/tmp/codex-wake-v0412-install/bin/python - <<'PY'
import importlib.metadata
print(importlib.metadata.version("codex-wake"))
PY
```

Result:

```text
0.4.12
```

Verify validated candidate help:

```text
/tmp/codex-wake-v0412-install/bin/codex-wake app candidates --help
```

Result included:

```text
--validate            check each candidate with thread/resume without
                      starting a turn
--only-idle           with --validate, only print candidates whose resumed
                      status is idle
```

Verify validated candidate JSON smoke:

```text
/tmp/codex-wake-v0412-install/bin/codex-wake app candidates --cwd "$PWD" --validate --only-idle --json --limit 2
```

Result: two local repo candidates returned `validation: resume_ok` and `status_type: idle`.

## Compatibility

- Wake record schema remains version `1`.
- App-server wake dispatch behavior is unchanged.
- Tmux dispatch behavior is unchanged.
- Only `stdio://` app-server dispatch is implemented.

## Known Gaps

- Public tag install smoke and GitHub release publication are pending tag push.
