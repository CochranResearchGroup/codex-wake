# v0.4.13 Release

Date: 2026-05-23

## Scope

Release the app-server service environment product hardening from
`3833e66 Harden app-server service command resolution`.

## Non-Goals

- Do not add new app-server transport modes.
- Do not change wake record schema version.
- Do not change tmux dispatch semantics.
- Do not cut a broader roadmap or packaging refactor.

## Current State

`main` contains the product hardening slice and CI has passed for commit
`3833e66`. The package version is still `0.4.12`; downstream repos need a
tagged release to install the service-environment fix by version.

## Acceptance Criteria

- `pyproject.toml` version is set to `0.4.13`.
- Release notes exist under `docs/releases/v0.4.13.md`.
- Source tests, compile checks, and diff checks pass.
- A wheel and source distribution build successfully.
- A temporary installed-wheel smoke verifies the new CLI surfaces.
- The release commit and `v0.4.13` tag are pushed.
- GitHub release exists for `v0.4.13`.
- Public tag install smoke confirms version `0.4.13`.

## Definition Of Done

This lane is complete when validation evidence is recorded under
`docs/dev/verification/`, CI passes on the release commit, the tag and GitHub
release are public, and the repo returns to a clean state.
